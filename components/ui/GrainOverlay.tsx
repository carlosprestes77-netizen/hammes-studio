// Film-grain overlay — a fixed, non-interactive noise layer over the whole
// frame. Gives the site the texture of a printed art publication.
export default function GrainOverlay() {
  return <div aria-hidden className="grain-overlay" />;
}
